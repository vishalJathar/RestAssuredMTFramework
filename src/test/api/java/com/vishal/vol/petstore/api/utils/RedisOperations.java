package com.vishal.vol.petstore.api.utils;

public class RedisOperations {

/*
	public static String getPinFromredis(String msisdn){


		/*String number= msisdn;
		Jedis jedis = new Jedis("172.16.5.35"); 
		String value=jedis.get("voltron_storefront_otp_"+number+"");
		if (value != null)
		{
			String otp=value.substring(value.length()-7);
			String otp1 = otp.substring(0,6);
			return otp1;
			System.out.println("Your OTP for "+number+" is => "+ otp1);

		}
		else
			System.out.println("OTP not available for "+number);

		System.out.println("Your OTP for "+number+" is : "+ jedis.get("voltron_storefront_otp_"+number+""));


	}*/

}


